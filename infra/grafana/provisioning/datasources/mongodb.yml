apiVersion: 1

datasources:
  - name: MongoDB
    type: grafana-mongodb-datasource
    access: proxy
    url: mongodb://mongodb:27017
    database: fango_logs
    jsonData:
      authType: none
      tlsAuth: false
      tlsAuthWithCACert: false
      tlsSkipVerify: true
    editable: true